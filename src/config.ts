/**
 * Site configuration.
 *
 * Change HOMEPAGE to swap what the root URL shows:
 *   "work"     → the work/portfolio listing (current default)
 *   "landing"  → the custom homepage (when ready)
 */
export const HOMEPAGE: "work" | "landing" = "work";
